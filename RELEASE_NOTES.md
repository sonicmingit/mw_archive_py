## 当前版本

- `v5.8.0`
- 更新说明见 [doc/logs/v5.8.0_update_log.md](doc/logs/v5.8.0_update_log.md)
- 本次重点：
  - 新增移动端 H5 页面，并接入主程序 `/h5` 路径直接访问；
  - 完成模型详情、附件下载、配置模型下载、收藏筛选、深色模式等移动端能力补齐；
  - 新增 PWA 支持，安卓与苹果可添加到主屏幕；
  - Docker 构建流程接入 `h5` 前端产物。
- 升级提醒：
  - 本次为功能增强版本，原有桌面端和 API 使用方式不变；
  - 若使用 Docker 部署，建议使用本次新镜像以获得 `/h5` 与 PWA 支持；
  - 若要在手机上安装 PWA，正式环境建议使用 `HTTPS` 访问。
